{
  "name": "nft",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nft",
    "create-local": "graph create --node http://localhost:8020/ nft",
    "remove-local": "graph remove --node http://localhost:8020/ nft",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nft"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.0",
    "@graphprotocol/graph-ts": "0.22.0"
  }
}
